programa {
  funcao inicio() {
     real base, altura, area

        escreva("Enter the  base of triangle: ")
        leia(base)

        escreva("Enter the height: ")
        leia(altura)

        area = (base * altura) / 2

        escreva("The area of triangle is: ", area)
  }
}
